# Legacy (MVP-era)

These files are kept for reference only and are not part of the current architecture.

- `brain_api_legacy.py`: MVP-era Brain that mixes decision logic with execution + wallet.
- `test_integration_legacy.py`: Integration script targeting `brain_api_legacy.py`.

The canonical Brain is now `brain/service.py` (stateless decision service).
